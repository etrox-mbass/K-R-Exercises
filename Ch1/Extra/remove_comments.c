#include <stdio.h>

/**
 * remove_comments: A program that removes all comments from a C program. Don't
 * forget the handle quoted strings and character constants properly. C
 * comments do not nest.
 */
