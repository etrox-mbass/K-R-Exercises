#include <stdio.h>

/**
 * entab: A program that replaces strings of blanks by the minimum number of
 * tab and blanks to achieve the same spacing. Use the same tab stops as for
 * detab program. When either a tab or a single blank would suffice to reach
 * a tab stop, which should be given preference?
 */
