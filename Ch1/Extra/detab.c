#include <stdio.h>

/**
 * detab: A program that replaces tabs in the input with the proper number of
 * blanks to space to the next tab stop. Assume a fixed set of tab stops, say
 * every n columns. Should n be a variable or a symbolic parameter?
 *
 * n should be a local variable instead of a symbolic parameter. You can supply
 * n through commandline argument.
 */


